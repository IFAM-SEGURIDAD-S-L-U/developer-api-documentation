description: >
  Forbidden – The user is not authorized to access the specified smartlock. This
  response may also appear if the request body is invalid or incomplete,
  preventing the server from resolving which smartlock is being referenced.
content:
  application/json:
    schema:
      type: object
      properties:
        success?:
          type: boolean
        reason:
          type: string
    example:
      success?: false
      reason: user-is-not-device-user
