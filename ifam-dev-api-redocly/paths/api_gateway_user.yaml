get:
  tags:
    - Gateway
  summary: Get the gateway by user
  description: >
    Returns the gateway associated with the user. This endpoint is used to retrieve
    the gateway information for the currently authenticated user.

  responses:
    '200':
      $ref: ../components/responses/GatewayByUserResponse.yaml
    '401':
      $ref: ../components/responses/Unauthorized.yaml
