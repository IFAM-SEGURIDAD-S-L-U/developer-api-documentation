get:
  tags:
    - Device
  summary: Get smartlock other keys
  description: >
    Use this endpoint to get the other keys of the smartlock with the specified
    `device_id`.      
  parameters:
    - name: device_id
      description: >
        The `device_id` that was returned by `/api/device/pair/encrypted` when
        the smartlock was paired.
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      $ref: ../../components/responses/getDeviceOtherKeysResponse.yaml
    '401':
      $ref: ../../components/responses/Unauthorized.yaml
    '403':
      $ref: ../../components/responses/UserNotDeviceUser.yaml
